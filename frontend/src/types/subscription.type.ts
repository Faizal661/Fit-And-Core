export interface SubscriptionData {
  trainerId: string;
  planDuration: string;
  amountInPaise: number;
  planName: string;
}

export interface subscriptionPlans {
  duration: string;
  amount: string;
  amountInPaise: number;
  savings: number;
}
